# Urban Residential Nighttime Thermal Comfort under Climate Change

[![DOI](https://img.shields.io/badge/Zenodo-doi-blue)](https://zenodo.org/) <!-- update DOI after upload -->

The code and data in this repository support the **peer review** of the manuscript:

**"Climate warming not only amplifies cooling demand but also undermines sleep comfort"**

## Purpose

This repository provides all code and external data needed to reproduce the figures and computational workflow described in the paper. Researchers can verify the SET (Standard Effective Temperature) computation pipeline, inspect per-capita discomfort hour calculations for 6 Chinese cities under 7 climate scenarios, and regenerate all figures (2–6).

## Online resources

- **Zenodo data package**: Full external inputs — EPW weather files, building energy data, shape coefficients, standard China maps, anonymised Weibo check-ins, cluster mappings, and prototype building SHP archives. See [Zenodo link → TBD] after publication.

## Workflow overview

```
Input data → code/run_all.py → result/27Degree/
                                    ↓
                          figure/*.py (Figures 2–6)

For Figure 6:
figure6/run_all.py → zhibiao + energy + load curves → figure/figure6-*.py
```

| Stage | Script | Description |
|-------|--------|-------------|
| **Step 1** | `code/pipeline/step1_compute_set.py` | Compute SET for every building × storey × hour; aggregate per-capita discomfort hours |
| **Step 2** | `code/pipeline/step2_pivot_tables.py` | Generate pivot tables |

## Repository layout

| Path | Contents |
|------|----------|
| **`code/`** | Main pipeline — config, core computation modules, pipeline steps, one-click runner |
| **`figure/`** | All figure plotting scripts (19 scripts, Figures 2–6), including grid export and public-reproducibility variants |
| **`figure6/`** | Figure 6 independent pipeline (SET → energy capacity → load curves) |
| **`input/`** | External data (download from Zenodo; see below) |
| **`result/`** | Pipeline outputs (generated by `code/run_all.py`) |

## Data in `input/`

Materials are organised for six pilot cities: **Beijing, Shanghai, Guangzhou, Shenzhen, Wuhan, Xiamen**.

| Directory | Contents | Source |
|-----------|----------|--------|
| **`input/epw/`** | EPW weather files — 6 cities × 7 climate scenarios (2020, 2040/2060 × RCP 2.6/4.5/8.5) | External |
| **`input/figure6/`** | Building annual cooling energy Excel (6 cities), energy capacity summary CSV, per-capita hours CSV, GW load curve caches (6 pickle files), provincial grid load shape | Pre-computed |
| **`input/shape_coefficient/`** | Building shape coefficient data — Guangzhou and Shenzhen | External |
| **`input/china_county_shp/`** | Standard map of China — county-level boundaries (审图号 GS(2024)0650) | External |
| **`input/china_city_boundary_shp/`** | City-level administrative boundaries | External |
| **`input/beijing_weibo/`** | Anonymised Sina Weibo summer check-in data (Beijing urban area); data description in file header | Collected |
| **`input/cluster_maps/`** | Building prototype cluster assignment tables for 6 cities | Pre-computed |
| **`input/china_city_shp/`** | Standard map of China — city-level boundaries (审图号 GS(2024)0650) | External |
| **`input/china_ninedash_shp/`** | Standard map of China — nine-dash line (审图号 GS(2024)0650) | External |

### GeoJSON grid data

Pre-computed, anonymised grid-level GeoJSON files are included under:

| Path | Description |
|------|-------------|
| `figure/figure2c_grid_data/` | Beijing 750m-grid heat data + basemap + Weibo points |
| `figure/figure2d_grid_data/` | 6-city grid heat data + core hull + core counties + index counties |
| `figure/figure5_grid_data/` | 6-city grid bivariate data (heat + vulnerability) + boundaries |

These GeoJSON files are pre-computed, anonymised grid data derived from private building SHP inputs. The `_public.py` figure scripts consume them directly and do **not** require the building SHP data.

## Data availability and legal notice

We are **prohibited** from distributing original, precise building-geometry datasets under the _Surveying and Mapping Law of the People's Republic of China_ and relevant national data security regulations.

- **Grid GeoJSON data** is the public-facing, coordinate-adjusted product derived from the SHP inputs. The `_public.py` figure scripts use only these GeoJSON files and are fully reproducible without the private SHP layers.
- **Weibo data** (`input/beijing_weibo/data.csv`) has been anonymised: every field except longitude, latitude, and venue type is replaced with `***` for all users except one reference record. A full data dictionary is included in the CSV file header.

## Code organisation

### `code/` — Main pipeline

| File | Role |
|------|------|
| `code/config/parameters.py` | Global parameters (temperature baseline, city configs, SET computation params) |
| `code/config/paths.py` | Path configuration — all paths are repo-relative |
| `code/core/set_calculator.py` | SET computation (pythermalcomfort), RH constraint (Magnus formula) |
| `code/core/epw_handler.py` | EPW weather file reader, pressure extraction, time-offset alignment |
| `code/core/city_matcher.py` | City name matching (pinyin / Chinese / English) |
| `code/core/excel_exporter.py` | Multi-sheet Excel export for per-hour SET results |
| `code/pipeline/step1_compute_set.py` | **Step 1**: Compute per-building per-storey hourly SET, aggregate per-capita discomfort hours |
| `code/pipeline/step2_pivot_tables.py` | **Step 2**: Generate cross-comparison pivot tables |
| `code/run_all.py` | Run Steps 1–2 sequentially |

### `figure6/` — Figure 6 pipeline

| File | Role |
|------|------|
| `figure6/step1_compute_set.py` | SET computation — 6 cities × 6 strategies |
| `figure6/step2_pivot_tables.py` | Pivot tables |
| `figure6/step3_energy_capacity.py` | Building energy consumption, cooling capacity, and peak load |
| `figure6/step4_citywide_load_curves.py` | City-wide GW-scale cooling load curves (generates `*_curves.pkl` caches) |
| `figure6/run_all.py` | Run Steps 1–4 sequentially |

### `figure/` — Figure scripts

| Figure | Scripts | Description |
|--------|---------|-------------|
| 2-b | `figure2-b-去除文字.py` | 6-city per-capita discomfort grouped bar chart |
| 2-c | `figure2-c.py` / `figure2-c_public.py` | Beijing Weibo heat-complaint validation overlay |
| 2-d | `figure2-d.py` / `figure2-d_public.py` | 6-city 3D stacked heat discomfort maps |
| 3-a | `figure3-a.py` | Guangzhou vs Shenzhen climate KDE comparison |
| 3-b | `figure3-b.py` | Guangzhou vs Shenzhen SET distribution |
| 3-c | `figure3-c.py` | Building shape coefficient KDE (GZ + SZ) |
| 4-l | `figure4-l.py` | Vertical thermal disparity — 3 Shenzhen archetypes |
| 4-r | `figure4-r.py` | 24h discomfort profiles — top floor vs non-top |
| 5 | `figure5.py` / `figure5_public.py` | 6-city 3D 2×2 bivariate maps |
| 6-a | `figure6-a.py` | Strategy trade-off: energy (TWh) vs discomfort (h) |
| 6-b | `figure6-b.py` | Cooling capacity density raincloud plots |
| 6-c | `figure6-c.py` | Typical midsummer city-wide cooling load curves |

**Figures 2-c, 2-d, 5** have two variants:
- `figureX.py` — Original version (requires private SHP data; for authors)
- `figureX_public.py` — Public version (reads only GeoJSON grid data; fully reproducible)

## Paths and local configuration

**Repository data paths are portable.** All scripts set `PROJECT_ROOT` relative to the script location (`os.path.dirname(os.path.abspath(__file__))`) and construct data paths with `os.path.join(PROJECT_ROOT, "input", ...)`. As long as the `input/` directory is present at the repository root, paths resolve correctly on any operating system.

**Pipeline output paths (pipeline-generated, not distributed).** Some figure scripts reference intermediate pipeline outputs under `E:\GeiMingHao_all\` (zhibiao, pop_lookup). These are **not part of this repository**; they are generated by running the figure6 pipeline (`figure6/run_all.py`) or the main pipeline (`code/run_all.py`). Run those pipelines first, then run the figure scripts.

## Replication instructions

### 1. Setup

```bash
git clone <this-repo>
cd thermal_comfort_analysis
pip install -r requirements.txt
```

### 2. Download external data

Download the **Zenodo data package** and extract it so that `input/` sits at the repository root.

The expected layout after extraction:

```
thermal_comfort_analysis/
├── input/          ← from Zenodo
│   ├── epw/
│   ├── figure6/
│   ├── shape_coefficient/
│   ├── china_county_shp/
│   ├── china_city_boundary_shp/
│   ├── beijing_weibo/
│   └── cluster_maps/
├── code/
├── figure/
├── figure6/
├── result/
└── ...
```

### 3. Run the main pipeline

```bash
python code/run_all.py
```

Generates: `result/27Degree/set_calculations/`, `result/27Degree/per_capita_hours/`, `result/27Degree/pivot_tables/`.

### 4. Run the Figure 6 pipeline

```bash
python figure6/run_all.py
```

Generates zhibiao CSV files, energy capacity summary, and load curve caches consumed by Figure 6 plotting scripts.

### 5. Generate figures

```bash
# Public-reproducible versions (use GeoJSON, no SHP needed)
python figure/figure2-c_public.py
python figure/figure2-d_public.py
python figure/figure5_public.py

# All other figures
python figure/figure2-b-去除文字.py
python figure/figure3-a.py
python figure/figure3-b.py
python figure/figure3-c.py
python figure/figure4-l.py
python figure/figure4-r.py
python figure/figure6-a.py
python figure/figure6-b.py
python figure/figure6-c.py
```

Output figures are saved under `figure/*_output/` directories.

### 6. Notes on the original (private-data) scripts

The `figureX.py` originals reference local paths to private building SHP and zhibiao data (`E:\GeiMingHao_all\`, `E:\cc data\...\population\`). These are **not distributed** and are retained only for author reference. Public reproducibility uses `figureX_public.py` with the provided GeoJSON grid data.

## Dependencies

| Package | Minimum | Purpose |
|---------|---------|---------|
| `pythermalcomfort` | 2.8 | SET computation |
| `numpy` | 1.24 | Array computation |
| `pandas` | 1.5 | Data I/O, CSV/Excel |
| `geopandas` | 0.12 | GIS processing |
| `matplotlib` | 3.6 | Plotting |
| `seaborn` | 0.12 | Statistical plots |
| `shapely` | 2.0 | Geometry operations |
| `openpyxl` | 3.0 | Excel I/O |
| `scikit-learn` | 1.2 | K-means (Figure 6-c) |

## Status

- Code and external data are complete for peer review.
- Additional QC utilities and the Shanghai full building inventory (~332 MB CSV) may be added as the release is finalised.

## Citation

If you use this code or data in your research, please cite:

> *[Paper citation — TBD after publication]*

## License

*[TBD]*
